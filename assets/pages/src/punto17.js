function ContarLetra(texto = ""){
    return palabra
if(typeof texto !== 'string'){
    return 'Error.. el valor ingresado no es una palabra';
}
if (texto === ''){
    return "No ingreso una palabra"
}

toLowerCase()
split('')
filter(letra => letra === 'a')
length;

}
console.log(`La letra "a" aparece ${cantidad} veces en "${palabra}".`);